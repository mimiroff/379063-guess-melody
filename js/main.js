
import {showElement} from './show-element';
import welcomeScreen from './welcome';
import artistScreen from './artist';
import genreScreen from './genre';
import resultOneScreen from './result-1';
import resultTwoScreen from './result-2';
import resultThreeScreen from './result-3';

showElement(resultThreeScreen);


  // const switchScreens = (direction) => {
  //   if (direction === `right` && screenNumber < sortedScreens.length - 1) {
  //     showElement(++screenNumber);
  //   } else if (direction === `left` && screenNumber > 0) {
  //     showElement(--screenNumber);
  //   }
  // };

