import assert from 'assert';

describe(`Answer generation`, () => {
  describe(`generateArtistQuestion function`, () => {
    it(`should return true`, () => {
      assert.equal(true, true);
    });
  });
});
